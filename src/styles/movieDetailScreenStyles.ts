import { StyleSheet } from "react-native";

export const MovieDetailScreenStyles = StyleSheet.create({
  image: {},
  subtitle: {
    fontSize: 10,
    fontWeight: "200",
    color: "#A0A0A0",
  },
});
