import { StyleSheet } from "react-native";

export const seriesScreenStyles = StyleSheet.create({
    container: {
        paddingTop: 57,
    },
    seriesContainer: {
        flexWrap: "wrap",
        flexDirection: "row",
        justifyContent: "space-around",
    }
})